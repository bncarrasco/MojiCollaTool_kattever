# ワーカー報告

各ワーカーは `TASK-XXX.md`を作成し、チャットだけでなくリポジトリ内へ正式結果を残す。

必須項目は司令役が各worktreeへ配置するlocal-onlyの`AGENTS.md`と個別worker promptを参照する。これらAI向け指示文書は報告commitへ含めない。未実施検証は `Not verified`、環境上不要な検証は理由付きで `N/A`とする。
